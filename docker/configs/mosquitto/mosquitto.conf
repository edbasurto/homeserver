listener 1883
listener 9001
protocol websockets

persistence true
persistence_location /mosquitto/data/

log_dest file /mosquitto/log/mosquitto.log
log_dest stdout

# Internal LAN only, not exposed via Traefik/reverse proxy — no auth
# configured yet. Revisit if this ever needs to be reachable off-LAN.
allow_anonymous true
